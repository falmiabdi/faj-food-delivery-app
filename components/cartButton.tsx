import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const CartButton = () => {
    const totalItems = 10;
    return (
        <TouchableOpacity>
            <Text>CarButton</Text>
        </TouchableOpacity>
    );
};

export default CartButton;